[project]
name = "srebook"
version = "0.1.3"
description = "Turns TIFF page scans into searchable, bookmarked, page-labeled PDFs"
requires-python = ">=3.11"
dependencies = [
    "pillow>=10",
    "pikepdf>=9",
    "lxml>=5",
    "numpy>=1.26",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pypdfium2>=4"]

[project.scripts]
srebook = "srebook.cli:main"

# gui-scripts, not scripts: on Windows this launches without a console window
# behind it, which is what archive staff should see.
[project.gui-scripts]
srebook-gui = "srebook.gui.app:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["srebook*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
